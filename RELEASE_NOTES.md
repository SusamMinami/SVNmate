# 一键更新SVN v1.4.3

本版本修复长期后台运行后托盘图标消失的问题，并降低空闲状态下的轮询频率。

## 托盘可靠性

- 监听 Windows `TaskbarCreated` 广播，Explorer 重启后自动重新注册托盘图标。
- 隐藏主窗口前检查现有托盘注册，丢失时自动补注册。
- 托盘恢复失败时保留主窗口并显示原因，避免程序进入无入口的后台状态。
- 保留重复启动 EXE 唤醒现有窗口的单实例行为。

## 后台运行

- 空闲日志检查由 200 毫秒调整为 1 秒，任务执行期间仍保持原刷新速度。
- 托盘操作检查由 100 毫秒调整为 250 毫秒。
- 定时任务检查由 1 秒调整为 5 秒，仍在设定分钟内执行。
- 主窗口隐藏时 DPI 检查由 1 秒调整为 10 秒，恢复窗口后回到 1 秒。
- 保留每日定时更新、模块更新检查和现有 SVN 执行流程。

## 安装

从 GitHub Release 下载 `SVNmate.zip`，解压后运行 `SVNAutoTool.exe`。

升级只替换程序文件；用户配置、日志和工具模块配置不会被覆盖。
