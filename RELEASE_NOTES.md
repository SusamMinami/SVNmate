# 一键更新SVN v1.4.4

本版本正式发布 SVNmate 外部更新接口与迁移核验助手模块，并补齐独立在线更新通道。

- 抽取无界面的 `svnmate_core`，统一负责 Update、Cleanup 和单次重试。
- SVNmate 常驻时开放 Windows Named Pipe，允许迁移核验助手请求更新指定目录。
- 外部更新返回逐目录、逐步骤的结构化结果。
- SVNmate 未运行时，迁移核验助手可直接使用同一 core；检测到运行中的旧版实例时
  不会并发执行。
- 新增迁移核验助手桌面程序：按源/海外 Jira 单号扫描 SVN 文件，检查目标本地修改、
  远端更新和海外提交版本。
- 支持 `res`、`doc`、`bin` 与 SVN externals，结果可筛选、复制路径和导出 JSON。
- 接入飞书合并表，支持 SERIA/OSCOA 双向识别，并区分国内迁移、纯海外、单提 OSOB
  和不合并任务；网络失败时使用本地缓存。
- 合并表任务支持多选批量流水线：统一扫描全部 Jira、一次调用 UE 迁移、按海外单号
  准备 TortoiseSVN 提交窗口，并在窗口关闭后统一复核。
- 批量迁移前按 `/Game/...` 层级显示去重资源目录树，支持文件夹三级选择；同一路径
  已由本批次其他 OSCOA 提交时显示“已提交”，不再重复迁移。
- 多工作副本与 externals 的 SVN 日志查询最多 4 路并发；同一工作副本内的多个
  Jira 仍合并为一条 `svn log` 命令。
- MigrationGuard v1.0.0 将源/目标工作区压缩为一行联动选择，固定国内 trunk、
  海外 trunk 和海外 OB 三套路径。
- 原“源单号/海外单号”输入合并为多行粘贴区，可从网页文本批量提取并显示映射结果。
- Trunk 表与海外 OB 表改为独立固定入口，自动读取工作簿最新可见页签。
- 海外 OB 流程在国内到海外 trunk 复核通过后，自动继续海外 trunk 到 OSOB，
  第一阶段未完成时不会误启动第二阶段。
- 新增 `migration-guard-latest` 独立发布通道，SVNmate 可单独安装和更新该模块。
- MigrationGuard v1.0.1 在更新前先从提交记录发现本批次文件，只检查并更新落后的
  文件目录；无需再更新整个 trunk 工作副本。
- MigrationGuard 增加固定阶段进度条；复核完成的文件持续标绿，整批完成后进度条
  转为绿色，存在待处理项或失败时分别显示橙色或红色。

# 一键更新SVN v1.4.3

本版本修复长期后台运行后托盘图标消失的问题，并降低空闲状态下的轮询频率。

## 托盘可靠性

- 监听 Windows `TaskbarCreated` 广播，Explorer 重启后自动重新注册托盘图标。
- 隐藏主窗口前检查现有托盘注册，丢失时自动补注册。
- 托盘恢复失败时保留主窗口并显示原因，避免程序进入无入口的后台状态。
- 保留重复启动 EXE 唤醒现有窗口的单实例行为。

## 后台运行

- 空闲日志检查由 200 毫秒调整为 1 秒，任务执行期间仍保持原刷新速度。
- 托盘操作检查由 100 毫秒调整为 250 毫秒。
- 定时任务检查由 1 秒调整为 5 秒，仍在设定分钟内执行。
- 主窗口隐藏时 DPI 检查由 1 秒调整为 10 秒，恢复窗口后回到 1 秒。
- 保留每日定时更新、模块更新检查和现有 SVN 执行流程。

## 安装

从 GitHub Release 下载 `SVNmate.zip`，解压后运行 `SVNAutoTool.exe`。

升级只替换程序文件；用户配置、日志和工具模块配置不会被覆盖。
