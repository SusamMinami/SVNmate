# 一键更新SVN v1.0.0

首个正式发布版本。

## 功能

- 批量执行 SVN Update / Cleanup。
- 支持双栏文件夹配置和勾选执行。
- 支持定时执行日常更新任务。
- 支持每日首次自动更新 `bin\WindowsNoEditor\Update.bat`。
- 支持 Cleanup 完成后自动执行 `res\Build.bat`。
- 实时输出执行过程，并在任务完成后显示完成状态。
- 支持音乐开关、昼夜主题、右下角签名和使用指南入口。

## 使用

下载 `一键更新SVN.zip`，解压后运行 `SVNAutoTool.exe`。

发布包文件：`一键更新SVN.zip`
